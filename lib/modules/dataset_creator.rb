# take in a hash of audiorecordings (with all attributes) and the saved_search.
# use the saved_search attributes to create the { :audiorecording, :start_offset, :end_offset } list

class DataSetCreator
  def execute(audio_readings, saved_search)

  end
end