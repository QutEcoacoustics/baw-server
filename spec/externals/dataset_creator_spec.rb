require File.dirname(__FILE__) + '/../../lib/modules/dataset_creator'

require 'spec_helper'

describe DataSetCreator do
  #it 'accepts audioreadings and a dataset as arguments' do
  #  dsc = DataSetCreator.new
  #  result = dsc.execute(AudioReading.all, {})
  #  result.should be(nil)
  #end
  #
  #it 'should return entire audio recording when dataset has no date or time restrictions' do
  #  # create the project, site, audio recording, audio event, tag objects and connections
  #  FactoryGirl.create(:write_permission)
  #
  #  dsc = DataSetCreator.new
  #  ar_all = AudioRecording.all.to_a.map(&:serializable_hash)
  #  dataset = Dataset.first.serializable_hash
  #  result = dsc.execute(ar_all, dataset)
  #
  #end
end