<%# <%== does not do html entity escaping %>
<%== @formatted_annotations.columns.join(', ') %>
<%==
     CSV.generate do |csv|
       @formatted_annotations.each do |row|
         csv << row.values
       end
     end
%>