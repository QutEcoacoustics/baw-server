- content_for :title, 'Assign Tag to Group'

= render partial: 'shared/admin/sidebar_tag_groups'

- content_for :right_sidebar do
  = render partial: 'shared/sidebar_metadata_heading', locals: {title: 'Tag Groups', icon_class: 'tags'}

= render 'form'